# 前端开发规范 — 入职办理

> 基于后端 HRMS-backend `OnboardingController` 实际接口编写，字段名与后端严格一致。
> 技术栈：React 18 + TypeScript + Umi 4 + Ant Design 5 + ProComponents + axios

---

## 一、菜单与路由

```
HR中控台（一级菜单，仅 HR/管理员角色可见）
└─ 入职办理  /hr/onboarding
```

```typescript
// .umirc.ts 或 config/routes.ts
{
  name: 'HR中控台',
  path: '/hr',
  access: 'canAdmin',
  routes: [
    { path: '/hr', redirect: '/hr/onboarding' },
    { name: '入职办理', path: '/hr/onboarding', component: './HR/Onboarding' },
  ],
},
```

```typescript
// src/access.ts
export default (initialState: any) => ({
  canAdmin: initialState?.currentUser?.userRole === 'admin',
});
```

---

## 二、目录结构

```
src/pages/HR/Onboarding/
├── index.tsx                    # 列表页（ProTable + Tab 状态筛选）
├── Detail.tsx                   # 详情页（只读展示 + 审批进度）
├── components/
│   ├── OnboardingFormModal.tsx  # 新增/编辑 表单弹窗
│   └── ConfirmModal.tsx         # HR确认到岗弹窗
├── services/
│   └── onboarding.ts            # API 接口封装
└── types/
    └── onboarding.ts            # TS 类型定义
```

---

## 三、API 接口速查

所有接口前缀为 `/api/onboarding`，需携带登录态 Cookie/Token。

| 方法 | 路径 | 请求参数 | 说明 |
|------|------|----------|------|
| GET | `/api/onboarding/list` | `?keyword=&statuses=DRAFT,APPROVING&page=1&size=10` | 分页列表，statuses 支持多选逗号分隔 |
| GET | `/api/onboarding/detail` | `?id=` | 详情 |
| POST | `/api/onboarding/draft` | Body: `OnboardingAddRequest` | 保存草稿（不发起审批） |
| POST | `/api/onboarding/submit` | Body: `OnboardingAddRequest` | 保存并提交审批 |
| POST | `/api/onboarding/{id}/submit` | — | 草稿提交审批 |
| PUT | `/api/onboarding/{id}` | Body: `OnboardingAddRequest` | 编辑草稿（已提交审批的不可编辑） |
| DELETE | `/api/onboarding/{id}` | — | 删除草稿（已提交审批的不可删除） |
| POST | `/api/onboarding/confirm` | `?id=&actualEntryDate=yyyy-MM-dd` | HR确认到岗，后端自动创建 employee + user |
| POST | `/api/onboarding/abandon` | `?id=` | 放弃入职（仅审批通过后可操作） |

### 响应格式

所有接口统一返回：

```typescript
{
  code: number;    // 0 = 成功
  data: T;         // 实际数据
  message: string; // 错误提示
}
```

---

## 四、TypeScript 类型定义

类型与后端实体字段**严格一一对应**，禁止自行改名。

```typescript
// src/pages/HR/Onboarding/types/onboarding.ts

/** 入职申请提交/编辑请求体 */
export interface OnboardingAddRequest {
  /** 候选人姓名 */
  candidateName: string;
  /** 联系手机号 */
  phone: string;
  /** 身份证号 */
  idCard?: string;
  /** 候选人邮箱 */
  email: string;
  /** 入职部门ID */
  deptId: number;
  /** 入职职位ID */
  positionId: number;
  /** 预定入职日期 yyyy-MM-dd */
  hireDate: string;
  /** 试用期月数，默认3 */
  probationMonth?: number;
  /** 录用类型: FULL_TIME / PART_TIME */
  employmentType: string;
  /** 合同类型: 1=固定期限 / 2=无固定期限 / 3=实习 */
  contractType?: number;
  /** 合同到期日 yyyy-MM-dd */
  contractExpireDate?: string;
  /** 审批流ID */
  flowId?: number;
  /** 约定基本工资 */
  baseSalary?: number;
  /** 社保缴纳基数 */
  socialInsuranceBase?: number;
  /** 公积金缴纳基数 */
  housingFundBase?: number;
  /** 工资卡账号 */
  bankAccount?: string;
  /** 开户行名称 */
  bankName?: string;
  /** 紧急联系人姓名 */
  emergencyContactName?: string;
  /** 紧急联系电话 */
  emergencyContactPhone?: string;
  /** 单据备注 */
  remark?: string;
}

/** 入职申请列表/详情 VO */
export interface OnboardingVO {
  id: number;
  businessNo: string;
  flowId?: number;
  recordId?: number;
  deptId: number;
  deptName?: string;
  positionId: number;
  positionName?: string;
  hireDate: string;
  probationMonth?: number;
  employmentType: string;
  contractType?: number;
  contractExpireDate?: string;
  baseSalary?: number;
  socialInsuranceBase?: number;
  housingFundBase?: number;
  bankAccount?: string;
  bankName?: string;
  candidateName: string;
  phone: string;
  idCard?: string;
  email?: string;
  emergencyContactName?: string;
  emergencyContactPhone?: string;
  employeeId?: number;
  operatorId: number;
  /** 审批人ID（部门负责人，选择部门后自动关联） */
  approverId?: number;
  /** 审批人姓名（列表/详情直接展示） */
  approverName?: string;
  remark?: string;
  createTime: string;
  updateTime: string;
  /** 审批状态: APPROVING / APPROVED / REJECTED / WITHDRAWN */
  approvalStatus?: string;
  /** 审批进度，如 "1/2" */
  approvalProgress?: string;
}

/** 分页响应 */
export interface PageVO<T> {
  records: T[];
  total: number;
  size: number;
  current: number;
  pages: number;
}
```

### 字段对照表（后端 Java → 前端 TS）

| 后端字段 (Java) | 前端字段 (TS) | 说明 |
|---|---|---|
| `candidateName` | `candidateName` | 候选人姓名 |
| `phone` | `phone` | 手机号 |
| `deptId` | `deptId` | 部门ID |
| `positionId` | `positionId` | 职位ID |
| `hireDate` | `hireDate` | 入职日期 yyyy-MM-dd |
| `employmentType` | `employmentType` | FULL_TIME / PART_TIME |
| `approverId` | `approverId` | 审批人ID（部门负责人） |
| `approverName` | `approverName` | 审批人姓名 |
| `approvalStatus` | `approvalStatus` | 审批状态 |
| `approvalProgress` | `approvalProgress` | 审批进度 |

---

## 五、API 接口封装

```typescript
// src/pages/HR/Onboarding/services/onboarding.ts
import request from '@/libs/request';

const BASE = '/api/onboarding';

/** 分页列表 */
export function listOnboarding(params: {
  keyword?: string;
  statuses?: string[];
  page: number;
  size: number;
}) {
  return request.get<{ code: number; data: PageVO<OnboardingVO>; message: string }>(
    `${BASE}/list`, { params }
  );
}

/** 详情 */
export function getOnboardingDetail(id: number) {
  return request.get<{ code: number; data: OnboardingVO; message: string }>(
    `${BASE}/detail`, { params: { id } }
  );
}

/** 保存草稿 */
export function saveDraft(data: OnboardingAddRequest) {
  return request.post(`${BASE}/draft`, data);
}

/** 保存并提交审批 */
export function submitOnboarding(data: OnboardingAddRequest) {
  return request.post(`${BASE}/submit`, data);
}

/** 草稿提交审批 */
export function submitDraft(id: number) {
  return request.post(`${BASE}/${id}/submit`);
}

/** 编辑草稿 */
export function updateOnboarding(id: number, data: OnboardingAddRequest) {
  return request.put(`${BASE}/${id}`, data);
}

/** 删除草稿 */
export function deleteOnboarding(id: number) {
  return request.delete(`${BASE}/${id}`);
}

/** HR确认到岗 */
export function confirmOnboarding(id: number, actualEntryDate: string) {
  return request.post(`${BASE}/confirm`, null, {
    params: { id, actualEntryDate },
  });
}

/** 放弃入职 */
export function abandonOnboarding(id: number) {
  return request.post(`${BASE}/abandon`, null, { params: { id } });
}
```

---

## 六、状态与颜色规范

| status | 含义 | 颜色 | 色值 | Tag |
|--------|------|------|------|-----|
| DRAFT | 草稿 | 灰色 | `#d9d9d9` | 草稿 |
| APPROVING | 审批中 | 蓝色 | `#1677ff` | 审批中 |
| APPROVED | 已批准待入职 | 绿色 | `#52c41a` | 已批准待入职 |
| REJECTED | 已拒绝 | 红色 | `#ff4d4f` | 已拒绝 |
| ONBOARDED | 已入职 | 绿色 | `#52c41a` | 已入职 |

```typescript
const STATUS_MAP: Record<string, { color: string; text: string }> = {
  DRAFT:      { color: '#d9d9d9', text: '草稿' },
  APPROVING:  { color: '#1677ff', text: '审批中' },
  APPROVED:   { color: '#52c41a', text: '已批准待入职' },
  REJECTED:   { color: '#ff4d4f', text: '已拒绝' },
  ONBOARDED:  { color: '#52c41a', text: '已入职' },
};
```

---

## 七、列表页操作按钮（按状态）

| 状态 | 可见操作 | 说明 |
|------|----------|------|
| DRAFT | 编辑 / 提交审批 / 删除 | 草稿可反复编辑、删除 |
| APPROVING | 查看审批进度 | 跳转审批中心详情页 |
| APPROVED | 确认入职 / 放弃 | 确认入职需选择实际入职日期 |
| REJECTED | 重新编辑 | 复用新增表单，提交后重新走审批 |
| ONBOARDED | 查看员工档案 | 已入职，链接到员工详情页 |

---

## 八、业务流程

```
列表页 → 点击"新增入职" → 表单弹窗
  ├─ 保存草稿 → DRAFT（可反复编辑/删除）
  └─ 提交审批 → APPROVING
       ├─ 审批中心通过 → APPROVED
       │   ├─ HR确认到岗 → 选日期 → ONBOARDED（后端自动创建 employee + user）
       │   └─ 放弃入职 → REJECTED
       └─ 审批中心拒绝 → REJECTED
            └─ 重新编辑 → 修改后重新提交审批
```

---

## 九、核心组件实现要点

### 9.1 列表页 `index.tsx`

```tsx
import { ProTable, type ProColumns, type ActionType } from '@ant-design/pro-components';
import { Button, Tag, message, Popconfirm, Tabs } from 'antd';
import { PlusOutlined } from '@ant-design/icons';
import { useRef, useState } from 'react';
import OnboardingFormModal from './components/OnboardingFormModal';
import ConfirmModal from './components/ConfirmModal';
import {
  listOnboarding, deleteOnboarding, submitDraft,
  abandonOnboarding,
} from './services/onboarding';
import type { OnboardingVO } from './types/onboarding';

const OnboardingPage: React.FC = () => {
  const actionRef = useRef<ActionType>();
  const [activeTab, setActiveTab] = useState('');
  const [formOpen, setFormOpen] = useState(false);
  const [editRecord, setEditRecord] = useState<OnboardingVO | null>(null);
  const [confirmOpen, setConfirmOpen] = useState(false);
  const [confirmId, setConfirmId] = useState<number>();

  const columns: ProColumns<OnboardingVO>[] = [
    { title: '单号', dataIndex: 'businessNo', width: 180, search: false },
    { title: '姓名', dataIndex: 'candidateName', width: 100, ellipsis: true },
    { title: '手机号', dataIndex: 'phone', width: 130, search: false },
    { title: '部门', dataIndex: 'deptName', width: 120, search: false },
    { title: '职位', dataIndex: 'positionName', width: 120, search: false },
    { title: '审批人', dataIndex: 'approverName', width: 100, search: false },
    {
      title: '录用类型', dataIndex: 'employmentType', width: 90, search: false,
      render: (_, r) => r.employmentType === 'FULL_TIME' ? '全职' : '兼职',
    },
    {
      title: '预定入职', dataIndex: 'hireDate', width: 110, search: false, valueType: 'date',
    },
    {
      title: '状态', dataIndex: 'approvalStatus', width: 120,
      render: (_, r) => {
        const cfg = STATUS_MAP[r.approvalStatus ?? 'DRAFT'];
        return cfg ? <Tag color={cfg.color}>{cfg.text}</Tag> : <Tag>草稿</Tag>;
      },
      valueType: 'select',
      valueEnum: {
        DRAFT: '草稿', APPROVING: '审批中', APPROVED: '已批准待入职',
        REJECTED: '已拒绝', ONBOARDED: '已入职',
      },
    },
    {
      title: '创建时间', dataIndex: 'createTime', width: 170, search: false, valueType: 'dateTime',
    },
    {
      title: '操作', key: 'action', width: 220, fixed: 'right', search: false,
      render: (_, r) => {
        const status = (r as any).approvalStatus; // 审批状态
        const isDraft = !r.recordId; // 无 recordId = 草稿
        if (isDraft) return (
          <>
            <a onClick={() => { setEditRecord(r); setFormOpen(true); }}>编辑</a>
            <a style={{ marginLeft: 8 }} onClick={() => submitDraft(r.id).then(() => actionRef.current?.reload())}>
              提交审批
            </a>
            <Popconfirm title="确定删除？" onConfirm={() => deleteOnboarding(r.id).then(() => actionRef.current?.reload())}>
              <a style={{ color: 'red', marginLeft: 8 }}>删除</a>
            </Popconfirm>
          </>
        );
        if (status === 'APPROVING') return (
          <a href={`/approval/detail/${r.recordId}`}>查看审批进度</a>
        );
        if (status === 'APPROVED') return (
          <>
            <a onClick={() => { setConfirmId(r.id); setConfirmOpen(true); }}>确认入职</a>
            <Popconfirm title="确定放弃？" onConfirm={() => abandonOnboarding(r.id).then(() => actionRef.current?.reload())}>
              <a style={{ color: '#faad14', marginLeft: 8 }}>放弃</a>
            </Popconfirm>
          </>
        );
        if (status === 'REJECTED') return (
          <a onClick={() => { setEditRecord(r); setFormOpen(true); }}>重新编辑</a>
        );
        return <span style={{ color: '#999' }}>已入职</span>;
      },
    },
  ];

  return (
    <div style={{ padding: 24, background: '#fff' }}>
      <ProTable<OnboardingVO>
        actionRef={actionRef}
        columns={columns}
        rowKey="id"
        params={{ statuses: activeTab ? [activeTab] : undefined }}
        request={async (p) => {
          const res = await listOnboarding({
            keyword: p.keyword as string,
            statuses: p.statuses as string[],
            page: p.current ?? 1,
            size: p.pageSize ?? 10,
          });
          return {
            data: res.data?.records ?? [],
            total: res.data?.total ?? 0,
            success: res.code === 0,
          };
        }}
        search={{ labelWidth: 'auto' }}
        toolbar={{
          actions: [
            <Button type="primary" icon={<PlusOutlined />}
              onClick={() => { setEditRecord(null); setFormOpen(true); }}>
              新增入职
            </Button>,
          ],
        }}
        headerTitle={
          <Tabs activeKey={activeTab}
            onChange={(key) => { setActiveTab(key); actionRef.current?.reload(); }}
            items={[
              { key: '', label: '全部' },
              { key: 'DRAFT', label: '草稿' },
              { key: 'APPROVING', label: '审批中' },
              { key: 'APPROVED', label: '已批准待入职' },
              { key: 'REJECTED', label: '已拒绝' },
              { key: 'ONBOARDED', label: '已入职' },
            ]}
          />
        }
      />
      <OnboardingFormModal open={formOpen} editData={editRecord}
        onCancel={() => { setFormOpen(false); setEditRecord(null); }}
        onOk={() => { setFormOpen(false); setEditRecord(null); actionRef.current?.reload(); }}
      />
      <ConfirmModal open={confirmOpen} onboardingId={confirmId}
        onOk={() => { setConfirmOpen(false); actionRef.current?.reload(); }}
        onCancel={() => setConfirmOpen(false)}
      />
    </div>
  );
};

export default OnboardingPage;
```

### 9.2 入职表单组件 `OnboardingFormModal.tsx`

核心要点：

1. **部门选择 → 自动关联审批人**：选择部门后，调用 `GET /api/department/detail?id=` 获取部门信息，将 `managerName` 展示在"审批人"字段旁
2. **字段完整性**：表单字段必须和后端 `OnboardingAddRequest` 完全对应
3. **日期格式**：`hireDate`、`contractExpireDate` 前端用 dayjs，提交时格式化为 `yyyy-MM-dd`
4. **手机号校验**：前端正则 `^1[3-9]\d{9}$`

```tsx
import {
  saveDraft, submitOnboarding, updateOnboarding,
} from '../services/onboarding';
import { getDepartmentDetail } from '@/services/department'; // 复用部门接口
import type { OnboardingAddRequest, OnboardingVO } from '../types/onboarding';
import {
  Modal, Form, Input, Select, DatePicker, InputNumber, message, Button, Descriptions,
} from 'antd';
import { useEffect, useState } from 'react';
import dayjs from 'dayjs';

interface Props {
  open: boolean;
  editData: OnboardingVO | null;
  onCancel: () => void;
  onOk: () => void;
}

const OnboardingFormModal: React.FC<Props> = ({ open, editData, onCancel, onOk }) => {
  const [form] = Form.useForm();
  const [loading, setLoading] = useState(false);
  const [deptList, setDeptList] = useState<{ label: string; value: number }[]>([]);
  const [posList, setPosList] = useState<{ label: string; value: number }[]>([]);
  const [approverName, setApproverName] = useState<string>(''); // 审批人姓名
  const isEdit = !!editData;

  useEffect(() => {
    if (open) {
      // 加载部门列表和职位列表（复用组织架构接口）
      fetchDeptList();
      fetchPosList();
      if (editData) {
        form.setFieldsValue({
          ...editData,
          hireDate: editData.hireDate ? dayjs(editData.hireDate) : null,
          contractExpireDate: editData.contractExpireDate ? dayjs(editData.contractExpireDate) : null,
        });
        if (editData.approverName) setApproverName(editData.approverName);
      } else {
        form.resetFields();
        setApproverName('');
      }
    }
  }, [open, editData, form]);

  /** 选择部门时自动获取部门负责人作为审批人 */
  const handleDeptChange = async (deptId: number) => {
    if (!deptId) {
      setApproverName('');
      return;
    }
    try {
      const res = await getDepartmentDetail(deptId);
      const managerName = res?.data?.managerName;
      setApproverName(managerName || '（该部门未设置负责人）');
    } catch {
      setApproverName('');
    }
  };

  const handleSubmit = async (submitNow: boolean) => {
    try {
      const values = await form.validateFields();
      setLoading(true);
      const payload: OnboardingAddRequest = {
        ...values,
        hireDate: values.hireDate?.format?.('YYYY-MM-DD') ?? values.hireDate,
        contractExpireDate: values.contractExpireDate?.format?.('YYYY-MM-DD') ?? values.contractExpireDate,
      };
      if (isEdit) {
        await updateOnboarding(editData!.id, payload);
        if (submitNow) await submitDraft(editData!.id);
      } else {
        if (submitNow) {
          await submitOnboarding(payload);
          message.success('已提交审批');
        } else {
          await saveDraft(payload);
          message.success('草稿已保存');
        }
      }
      onOk();
    } catch (e: any) {
      if (e?.message) message.error(e.message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <Modal
      title={isEdit ? '编辑入职申请' : '新增入职申请'}
      open={open} onCancel={onCancel} width={720} destroyOnClose
      footer={isEdit ? [
        <Button key="cancel" onClick={onCancel}>取消</Button>,
        <Button key="save" type="primary" loading={loading} onClick={() => handleSubmit(false)}>保存</Button>,
      ] : [
        <Button key="cancel" onClick={onCancel}>取消</Button>,
        <Button key="draft" loading={loading} onClick={() => handleSubmit(false)}>保存草稿</Button>,
        <Button key="submit" type="primary" loading={loading} onClick={() => handleSubmit(true)}>提交审批</Button>,
      ]}
    >
      <Form form={form} layout="vertical" style={{ marginTop: 12 }}
        initialValues={{ probationMonth: 3, employmentType: 'FULL_TIME' }}>

        {/* ---- 基本信息 ---- */}
        <Form.Item name="candidateName" label="姓名"
          rules={[{ required: true, message: '必填' }]}>
          <Input placeholder="候选人姓名" maxLength={64} />
        </Form.Item>
        <Form.Item name="phone" label="手机号"
          rules={[
            { required: true, message: '必填' },
            { pattern: /^1[3-9]\d{9}$/, message: '手机号格式不正确' },
          ]}>
          <Input placeholder="11位手机号" maxLength={11} />
        </Form.Item>
        <Form.Item name="email" label="邮箱"
          rules={[{ required: true, message: '必填' }, { type: 'email', message: '格式不正确' }]}>
          <Input placeholder="候选人邮箱" />
        </Form.Item>
        <Form.Item name="idCard" label="身份证号">
          <Input placeholder="身份证号（选填）" maxLength={18} />
        </Form.Item>

        {/* ---- 部门（选择后自动关联审批人） ---- */}
        <Form.Item name="deptId" label="所属部门"
          rules={[{ required: true, message: '必选' }]}>
          <Select placeholder="选择部门" showSearch
            optionFilterProp="label" options={deptList}
            onChange={handleDeptChange}
          />
        </Form.Item>

        {/* ---- 审批人（自动展示，不可编辑） ---- */}
        <Form.Item label="审批人">
          <Input value={approverName} disabled
            placeholder="选择部门后自动关联负责人"
            style={{ color: approverName ? '#000' : '#999' }}
          />
        </Form.Item>

        <Form.Item name="positionId" label="职位"
          rules={[{ required: true, message: '必选' }]}>
          <Select placeholder="选择职位" showSearch
            optionFilterProp="label" options={posList}
          />
        </Form.Item>

        <Form.Item name="employmentType" label="录用类型"
          rules={[{ required: true, message: '必选' }]}>
          <Select options={[
            { label: '全职', value: 'FULL_TIME' },
            { label: '兼职', value: 'PART_TIME' },
          ]} />
        </Form.Item>

        <Form.Item name="hireDate" label="预定入职日期"
          rules={[{ required: true, message: '必选' }]}>
          <DatePicker style={{ width: '100%' }} placeholder="选择日期" />
        </Form.Item>

        <Form.Item name="probationMonth" label="试用期(月)">
          <InputNumber min={0} max={12} style={{ width: '100%' }} placeholder="默认3个月" />
        </Form.Item>

        {/* ---- 合同信息 ---- */}
        <Form.Item name="contractType" label="合同类型">
          <Select options={[
            { label: '固定期限', value: 1 },
            { label: '无固定期限', value: 2 },
            { label: '实习', value: 3 },
          ]} />
        </Form.Item>
        <Form.Item name="contractExpireDate" label="合同到期日">
          <DatePicker style={{ width: '100%' }} />
        </Form.Item>

        {/* ---- 薪资信息 ---- */}
        <Form.Item name="baseSalary" label="基本工资">
          <InputNumber min={0} step={100} style={{ width: '100%' }}
            prefix="¥" placeholder="约定基本工资" />
        </Form.Item>
        <Form.Item name="socialInsuranceBase" label="社保基数">
          <InputNumber min={0} step={100} style={{ width: '100%' }} prefix="¥" />
        </Form.Item>
        <Form.Item name="housingFundBase" label="公积金基数">
          <InputNumber min={0} step={100} style={{ width: '100%' }} prefix="¥" />
        </Form.Item>

        {/* ---- 银行卡 ---- */}
        <Form.Item name="bankAccount" label="工资卡账号">
          <Input placeholder="银行卡号" maxLength={32} />
        </Form.Item>
        <Form.Item name="bankName" label="开户行">
          <Input placeholder="开户行名称" maxLength={128} />
        </Form.Item>

        {/* ---- 紧急联系人 ---- */}
        <Form.Item name="emergencyContactName" label="紧急联系人">
          <Input placeholder="姓名" maxLength={64} />
        </Form.Item>
        <Form.Item name="emergencyContactPhone" label="紧急联系电话">
          <Input placeholder="手机号" maxLength={20} />
        </Form.Item>

        {/* ---- 备注 ---- */}
        <Form.Item name="remark" label="备注">
          <Input.TextArea placeholder="单据备注" maxLength={512} rows={2} />
        </Form.Item>
      </Form>
    </Modal>
  );
};

export default OnboardingFormModal;
```

### 9.3 确认入职弹窗 `ConfirmModal.tsx`

```tsx
import { confirmOnboarding } from '../services/onboarding';
import { Modal, Form, DatePicker, message } from 'antd';
import { useState } from 'react';
import dayjs from 'dayjs';

interface Props {
  open: boolean;
  onboardingId?: number;
  onOk: () => void;
  onCancel: () => void;
}

const ConfirmModal: React.FC<Props> = ({ open, onboardingId, onOk, onCancel }) => {
  const [form] = Form.useForm();
  const [loading, setLoading] = useState(false);

  const handleOk = async () => {
    try {
      const values = await form.validateFields();
      const dateStr = values.actualEntryDate.format('YYYY-MM-DD');
      setLoading(true);
      await confirmOnboarding(onboardingId!, dateStr);
      message.success('确认入职成功，已自动创建员工档案和登录账号');
      onOk();
    } catch (e: any) {
      if (e?.message) message.error(e.message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <Modal title="确认入职" open={open} onOk={handleOk} onCancel={onCancel}
      confirmLoading={loading} destroyOnClose width={420}>
      <Form form={form} layout="vertical" style={{ marginTop: 16 }}
        initialValues={{ actualEntryDate: dayjs() }}>
        <Form.Item name="actualEntryDate" label="实际入职日期"
          rules={[{ required: true, message: '请选择' }]}>
          <DatePicker style={{ width: '100%' }} />
        </Form.Item>
      </Form>
      <p style={{ color: '#666', fontSize: 12 }}>
        确认后系统将自动：生成工号 → 创建员工档案 → 创建系统登录账号
      </p>
    </Modal>
  );
};

export default ConfirmModal;
```

---

## 十、审批人关联功能说明

**后端逻辑**：新增/编辑入职申请时，后端根据 `deptId` 自动查询 `department.managerId`，写入 `emp_onboarding.approverId`。

**前端对接**：
1. 表单中选择部门后，调用 `GET /api/department/detail?id=xxx` 获取 `managerName`
2. 将负责人姓名展示在"审批人"字段（只读、灰色背景）
3. 若部门未设置负责人，显示"（该部门未设置负责人）"
4. 提交时无需传 `approverId`，后端自动处理
5. 列表/详情直接展示 `approverName` 字段即可

---

## 十一、异常处理

| 场景 | 前端处理 |
|------|----------|
| 401 未登录 | `request.ts` 拦截器统一跳转 `/user/login` |
| 非 HR 角色 | 路由 `access: 'canAdmin'` 自动隐藏菜单 |
| 已提交审批的申请被编辑 | 后端返回 `OPERATION_ERROR`，前端 `message.error` 提示 |
| 手机号格式错误 | 前端 `pattern` 校验 + 后端双重校验 |
| 部门未设置负责人 | 审批人显示提示文字，提交审批时后端会报错提示 |
| 审批流未配置 | 后端返回 `NOT_FOUND_ERROR`，前端提示联系管理员 |

---

## 十二、核心开发规范

1. **字段名严禁自行发挥**——TS 类型字段名必须与后端 Java 实体字段名一致（camelCase），否则联调必挂
2. **状态判断用 `recordId` 是否为 null**——`recordId == null` 表示草稿，`recordId != null` 时看 `approvalStatus` 判断后续状态
3. **日期统一 `yyyy-MM-dd` 字符串传输**——不用时间戳，不用 ISO 格式
4. **审批进度复用现有审批中心**——`/approval/detail/:recordId` 页面已存在，入职模块直接跳转即可
5. **HR确认到岗后自动创建三表**——employee + employee_detail + user，前端只需刷新列表
